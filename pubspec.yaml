name: my_meals_3
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.11.0
  shared_preferences: ^2.2.2
  image: ^4.5.0
  image_picker: ^1.1.2
  path_provider: ^2.0.15
  path: ^1.8.2
  camera: ^0.11.0
  fluttertoast: ^8.2.6
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/app_logo.png
    - assets/models2/classification_model.tflite
    - assets/models2/nutrition_model.tflite
    - assets/models2/DeepLabV3.tflite
    - assets/raw/
    - assets/raw/c1_classes.txt
    - assets/raw/food_recommendation.txt
